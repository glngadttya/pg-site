<header class="sticky top-0 z-50 bg-[--yellow] border-b-4 border-[--ink]">
  <div class="max-w-6xl mx-auto flex items-center justify-between gap-3 px-4 py-3">
    <a href="/" class="font-display text-xl md:text-2xl tracking-tight brutal-sm bg-[--ink] text-[--paper] px-3 py-1">VANPAY</a>
    <div class="flex items-center gap-2">
      <button id="menu-btn" class="btn btn-ink px-3 py-2" aria-label="Buka menu" aria-expanded="false">
        <svg id="icon-hamburger" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="square"><line x1="3" y1="6" x2="21" y2="6"/><line x1="3" y1="12" x2="21" y2="12"/><line x1="3" y1="18" x2="21" y2="18"/></svg>
        <svg id="icon-close" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="square" class="hidden"><line x1="6" y1="6" x2="18" y2="18"/><line x1="18" y1="6" x2="6" y2="18"/></svg>
      </button>
    </div>
  </div>
</header>

<% var p = (typeof path !== 'undefined') ? path : (typeof reqPath !== 'undefined' ? reqPath : ''); %>
<% function isActive(prefix, exact) { if (exact) return p === prefix; return p.indexOf(prefix) === 0; } %>
<div id="menu-backdrop" class="fixed inset-0 bg-black/40 z-[55] hidden"></div>
<aside id="side-menu" class="fixed top-0 right-0 h-full w-72 max-w-[85vw] bg-[--paper] border-l-4 border-[--ink] z-[60] p-5 space-y-2 translate-x-full transition-transform duration-300 ease-out overflow-y-auto">
  <div class="flex items-center justify-between mb-4">
    <span class="font-display text-lg tracking-tight">MENU</span>
    <button id="close-btn" class="btn btn-pink px-3 py-1.5 text-sm" aria-label="Tutup menu"><i class="fa-solid fa-xmark mr-1"></i>Tutup</button>
  </div>
  <% if (user) { %>
    <a href="/" class="nav-item <%= isActive('/', true) ? 'active' : '' %>"><% if (isActive('/', true)) { %><span class="nav-marker">▸</span><% } %><i class="fa-solid fa-house"></i>Beranda</a>
    <a href="/dashboard" class="nav-item <%= isActive('/dashboard', true) ? 'active' : '' %>"><% if (isActive('/dashboard', true)) { %><span class="nav-marker">▸</span><% } %><i class="fa-solid fa-gauge-high"></i>Dashboard</a>
    <a href="/withdraw" class="nav-item <%= isActive('/withdraw', true) ? 'active' : '' %>"><% if (isActive('/withdraw', true)) { %><span class="nav-marker">▸</span><% } %><i class="fa-solid fa-money-bill-transfer"></i>Tarik Dana</a>
    <a href="/developer" class="nav-item <%= isActive('/developer', true) ? 'active' : '' %>"><% if (isActive('/developer', true)) { %><span class="nav-marker">▸</span><% } %><i class="fa-solid fa-code"></i>Developer</a>
    <a href="/history" class="nav-item <%= isActive('/history', true) ? 'active' : '' %>"><% if (isActive('/history', true)) { %><span class="nav-marker">▸</span><% } %><i class="fa-solid fa-clock-rotate-left"></i>Riwayat</a>
    <a href="/docs" class="nav-item <%= isActive('/docs', true) ? 'active' : '' %>"><% if (isActive('/docs', true)) { %><span class="nav-marker">▸</span><% } %><i class="fa-solid fa-book"></i>Docs</a>
    <% if (user.role === 'owner') { %>
      <a href="/admin/dashboard" class="nav-item <%= isActive('/admin', false) ? 'active' : '' %>"><% if (isActive('/admin', false)) { %><span class="nav-marker">▸</span><% } %><i class="fa-solid fa-user-shield"></i>Admin</a>
    <% } %>
    <a href="/auth/logout" class="nav-item"><i class="fa-solid fa-right-from-bracket"></i>Logout</a>
  <% } else { %>
    <a href="/" class="nav-item <%= isActive('/', true) ? 'active' : '' %>"><% if (isActive('/', true)) { %><span class="nav-marker">▸</span><% } %><i class="fa-solid fa-house"></i>Beranda</a>
    <a href="/docs" class="nav-item <%= isActive('/docs', true) ? 'active' : '' %>"><% if (isActive('/docs', true)) { %><span class="nav-marker">▸</span><% } %><i class="fa-solid fa-book"></i>Docs</a>
    <a href="/auth" class="nav-item <%= isActive('/auth', false) ? 'active' : '' %>"><% if (isActive('/auth', false)) { %><span class="nav-marker">▸</span><% } %><i class="fa-solid fa-right-to-bracket"></i>Masuk</a>
  <% } %>
</aside>

<script>
  (function () {
    var menuBtn = document.getElementById('menu-btn');
    var closeBtn = document.getElementById('close-btn');
    var menu = document.getElementById('side-menu');
    var backdrop = document.getElementById('menu-backdrop');
    var iconOpen = document.getElementById('icon-hamburger');
    var iconClose = document.getElementById('icon-close');
    function open() {
      menu.classList.remove('translate-x-full');
      backdrop.classList.remove('hidden');
      menuBtn.setAttribute('aria-expanded', 'true');
      iconOpen.classList.add('hidden');
      iconClose.classList.remove('hidden');
    }
    function close() {
      menu.classList.add('translate-x-full');
      backdrop.classList.add('hidden');
      menuBtn.setAttribute('aria-expanded', 'false');
      iconOpen.classList.remove('hidden');
      iconClose.classList.add('hidden');
    }
    menuBtn.addEventListener('click', function () {
      menu.classList.contains('translate-x-full') ? open() : close();
    });
    closeBtn.addEventListener('click', close);
    backdrop.addEventListener('click', close);
    document.addEventListener('keydown', function (e) {
      if (e.key === 'Escape') close();
    });
  })();
</script>